# OJO, EN ESTE PROYECTO TODO LO TENGO EJECUTAR COMO SUDO

# Hay que arrancar desde la terminal de fuera el proyecto 
````
npm run start:dev
````

# Backend en Nest
# Desde la terminal del proyecto
````
docker compose up -d
````

Copiar el ```.env.tempalate``` y renombrarlo a .env

